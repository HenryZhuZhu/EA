# EA Platform V1.0.3 — 2026-07-14 上午修改交接

> 面向：产品、设计、前端开发、测试  
> 修改时段：2026-07-14 上午  
> 当前版本：V1.0.3  
> 源码目录：`app/`  
> 发布产物：`V1.0.3.html`、`web/`  
> 状态：已完成构建并同步发布

---

## 1. 本次修改目标

本轮主要围绕以下四个方向：

1. 统一“新建 Case”和“我的 Case”的思维导图体验。
2. 为新建 Case 增加可直接操作的 Spotlight 新手引导。
3. 统一任务卡、思路卡、连线、图例、附件和语音备注表现。
4. 精简 Case 产品标签体系，让产品 Pill 始终伴随 L 层级标签。

---

## 2. 新建 Case 思维导图

核心文件：`app/src/app/components/CreateCaseMindMapFlow.tsx`

### 2.1 默认状态

- 进入新建 Case 后，思维导图默认创建一个思路节点并进入编辑状态。
- 不再显示独立的大型“新建思路”占位卡。
- 默认思路分类为“测试”。
- 分类支持：设计、工艺、测试、其他。
- 选择“其他”时，只保留一个“工作思路”文本框。
- 思路与 Case 根节点距离已缩短，初始状态下两个节点均尽量显示在可视区域。
- 编辑中的节点不可拖动，输入框使用 `nodrag nopan`，避免 React Flow 拖拽拦截输入。

### 2.2 思路填写逻辑

标准分类使用三段结构：

- 因为：必填。
- 原理说明：选填，提示“补充原理内容，可增加Case评分”。
- 所以：必填。

保存文本由 `composeThought()` 组合，编辑时由 `parseThought()` 拆分。

注意：

- 修复了填写“因为”时“所以”被同步填入的状态问题。
- 保存思路后不再自动创建任务。
- 用户必须点击思路卡右上角蓝色加号，手动创建执行任务。

### 2.3 任务创建

- 点击思路卡蓝色加号后创建一个任务，并自动进入任务编辑状态。
- 任务默认紧急程度为“一般”。
- 任务保存后才进入下一个引导步骤。
- 附件和语音备注并排显示。
- 已取消无消费场景的“附言”文本框。
- 显示态任务卡与“我的 Case”思维导图任务卡保持一致：
  - 256px 默认宽度。
  - 左侧紧急度色条。
  - 状态图标。
  - 预期内容。
  - 悬停展开负责人、部门、完成时间、附件和语音播放。

### 2.4 子思路

- 任务卡右上角蓝色加号用于添加子思路。
- 新建子思路时不自动创建子任务。
- 用户完成子思路后，再手动点击蓝色加号创建对应任务。

---

## 3. 思维导图布局和视觉统一

核心文件：

- `app/src/app/components/CreateCaseMindMapFlow.tsx`
- `app/src/app/components/MindMapFlow.tsx`

### 3.1 工具栏

新建 Case 已按“我的 Case”同步：

- 全局视图。
- 只看未完成。
- 只看有效路径。
- 只看我的。
- 状态筛选。
- 部门筛选。
- 横向/纵向布局。
- 收起已完成。
- 手动拖动后重置布局。
- 全屏。

已移除：

- 顶部“添加思路”。
- 顶部“新手引导”。
- React Flow 锁定按钮。
- 重播引导按钮。
- 图例显示/隐藏按钮。

### 3.2 全屏

- 新建 Case 恢复全屏按钮。
- 点击进入全屏，再次点击退出。

### 3.3 缩略图

统一颜色逻辑：

- Case 根节点：深灰色 `#334155`。
- 思路节点：琥珀色 `#F59E0B`。
- 执行任务：品牌蓝 `#0052D9`。
- 未知节点：浅灰色 `#CBD5E1`。

缩略图支持点击、拖动快速定位。

### 3.4 连线

- 统一使用 React Flow `smoothstep`。
- 圆角：`borderRadius: 10`。
- 偏移：`offset: 28`。
- 使用共享主干，减少同级节点之间的平行线重叠。
- 任务始终位于思路右侧；纵向模式相应旋转布局方向。

### 3.5 编辑态避让

“我的 Case”中，卡片进入编辑态变大后，会重新计算布局并推开其他节点，避免覆盖。

---

## 4. Spotlight 新手引导

核心文件：`app/src/app/components/CreateCaseMindMapFlow.tsx`

### 4.1 触发方式

- 进入新建 Case 时立即出现。
- 当前持久化 Key：`ea_mm_create_spotlight_v12`。
- “下次不再显示”默认勾选。
- 关闭时仅在勾选状态下写入 `localStorage`。
- 如果引导内容有版本更新，需要升级 Key，否则已完成旧引导的浏览器不会重新显示。

> 开发注意：上午调试中多次出现“看不到引导”，原因都是浏览器保留了上一版 Key。后续建议将引导版本集中配置，并增加仅开发/演示使用的“重播引导”入口，但不要重新放回正式工具栏。

### 4.2 当前 17 步顺序

1. 全屏查看。
2. 缩略图。
3. 放大画布。
4. 缩小画布。
5. 适配视图。
6. 顶部工具栏。
7. 认识 Case 根节点。
8. 选择思路分类。
9. 填写怀疑依据（因为）。
10. 填写怀疑结论（所以）。
11. 填写首屏预置的第一个任务。
12. 查看任务卡左侧的紧急程度色条。
13. 理解紧急程度图例与色条的对应关系。
14. 点击思路卡蓝色加号，再添加一个任务。
15. 填写新任务。
16. 添加更多并列思路。
17. 添加更多细节思路（子思路）。

### 4.3 核心交互规则

- 分类步骤允许用户自由选择，不强制“测试”。
- “因为”为空时不能进入下一步。
- “所以”为空时不能完成思路。
- 首次进入时直接显示 Case 根节点、一个思路和一个任务。
- 首个任务随画布初始化预置，但必须在完成思路后由用户填写并确认。
- 首个任务完成后，再引导用户点击思路卡真实蓝色加号添加第二个任务。
- 第二个任务仍需填写并点击真实确认按钮。
- 最后一步按钮文案为“完成”。
- 已完成步骤使用灰蓝色勾选圆点，可点击返回。

### 4.4 当前关键文案

- 缩略图：“点击拖动，快速定位。”
- 添加更多任务：“点击思路卡上的蓝色加号，再添加一个任务。”
- 填写第一个任务：“填写相关信息，点击确认，创建任务。”
- 图例：“图例颜色对应任务卡左侧色条，快速识别紧急程度。”
- 添加更多细节思路：“点击蓝色加号，添加更多子思路。”

### 4.5 Spotlight 视觉实现

- 遮罩限制在 React Flow 画布可视区域内。
- 使用单一圆角挖空区域，不再使用四块矩形遮罩，避免四角漏光。
- 使用品牌蓝/天蓝双层柔光和内描边。
- 引导浮窗会避让被介绍的整张任务卡。
- 高亮目标和引导卡会根据 `getBoundingClientRect()` 持续更新位置。

### 4.6 过渡动画

通用步骤：

- 聚光区域约 680ms。
- 引导浮窗约 760ms。

关键过渡：

1. 第 11→12 步保存首个任务后等待 900ms；聚光先跟随同一任务卡自然收起，再用 900ms 收缩到左侧紧急程度色条，引导浮窗始终以整张任务卡避让定位。
2. 第 12→13 步从色条移动到图例时，聚光区域用 1200ms、引导浮窗用 1300ms，并用连线说明色条与图例的对应关系。
3. 第 14→15 步点击蓝色加号后等待 520ms，待新任务节点渲染和画布适配稳定后，再用 900ms 将聚光移到新任务编辑卡。
4. 第 15→16 步保存第二个任务后等待 760ms，再慢速移动到“新建思路”。
5. 图例、添加任务、新任务、添加更多思路步骤不执行 `scrollIntoView()`，避免与 React Flow 的 `fitView()` 同时移动产生跳跃。
6. 第 13→14、15→16、16→17 等跨画布过渡统一使用 1200ms 聚光与 1300ms 引导浮窗动画。
7. 第 17 步只定位刚保存的第二个任务，不给多个任务同时挂载相同引导目标。

### 4.7 图例联动

- 图例始终显示，不提供工具栏隐藏按钮。
- 图例介绍位于创建第一个任务之后。
- 当前任务紧急程度对应的图例项会高亮。
- 任务卡整体轻微高亮。
- 任务卡左侧紧急度色条轻微闪动。
- 图例和任务卡色条之间绘制天蓝色发光虚线，并高亮两个端点。
- 色条通过 `data-create-guide="urgency-bar"` 提供定位锚点。

---

## 5. 图例

适用文件：

- `app/src/app/components/MindMapFlow.tsx`
- `app/src/app/components/CreateCaseMindMapFlow.tsx`

当前逻辑：

- “我的 Case”和“新建 Case”均显示同一套紧急度图例。
- 图例固定显示在左下角控件右侧。
- 不再提供显示/隐藏图例按钮。
- “只看我的”启用时，图例显示“已聚焦我的”。
- 图例颜色与任务卡左侧紧急度色条一一对应：
  - 非常紧急：红色。
  - 紧急：橙色。
  - 一般：天蓝色。
  - 不紧急：灰色。

---

## 6. 卡片和任务数据调整

### 6.1 取消退回重做

已整体移除：

- 返工标记。
- “退回重做”入口。
- 返工派生任务及相关展示逻辑。

涉及文件主要包括：

- `app/src/app/components/data.ts`
- `app/src/app/components/MindMapFlow.tsx`
- `app/src/app/components/TaskDetail.tsx`
- `app/src/app/components/CaseDetail.tsx`

### 6.2 取消附言

- 任务创建不再显示附言文本框。
- 附件成为独立字段。
- 附件与语音备注并排。
- 任务卡和任务详情同步展示附件/语音。

涉及文件：

- `app/src/app/components/AddTaskModal.tsx`
- `app/src/app/components/CreateCaseMindMapFlow.tsx`
- `app/src/app/components/MindMapFlow.tsx`
- `app/src/app/components/TaskDetail.tsx`
- `app/src/app/components/MindMap.tsx`

### 6.3 语音示例

- 在首个 DDR5 示例 Case 中增加可播放的离线语音备注。
- `demoVoiceNote()` 在浏览器中生成约 2 秒 WAV Data URL，不依赖网络。
- 播放控件会在任务卡悬停展开区和任务详情出现。

涉及文件：

- `app/src/app/components/data.ts`
- `app/src/app/components/VoiceNote.tsx`

---

## 7. 产品 Pill 与 L 标签

新增组件：`app/src/app/components/CaseProductPill.tsx`

### 7.1 规则

- 产品 Pill 必须紧跟在 Case 的 L1–L5 标签旁。
- 蓝色圆角 Pill，带产品图标和产品型号。
- 优先通过 `caseProductOf()` 获取正式产品代码。
- 如果产品不在 `mockProducts`，回退显示 Case 原始 `product`。
- 没有关联产品时显示“未关联产品”。
- 支持标准尺寸和 `compact` 紧凑尺寸。

### 7.2 已覆盖页面

- Case 详情。
- Case 列表的多个视图。
- 我的待办/我的 Case。
- 任务详情。
- 新建 Case 相似案例。
- 我的 Case 思维导图。
- 新建 Case 思维导图。
- 旧版 MindMap。
- Dashboard。
- 分类时间线。
- 个人页。
- 审批列表及审批详情。
- 演示模式。
- Case 相关推荐。

主要消费文件：

- `CaseDetail.tsx`
- `CaseList.tsx`
- `MyTodo.tsx`
- `TaskDetail.tsx`
- `CreateCase.tsx`
- `MindMapFlow.tsx`
- `CreateCaseMindMapFlow.tsx`
- `MindMap.tsx`
- `Dashboard.tsx`
- `CategoryTimeline.tsx`
- `ProfilePage.tsx`
- `MyApprovals.tsx`
- `DemoMode.tsx`

### 7.3 已移除的旧标签

Case 详情页顶部已移除：

- PAL。
- 产品阶段，例如 FSO。
- ObjectType，例如 Die。
- 来源，例如“来源·检查项Fail”。
- Issue 编号胶囊，例如 ISS-20260415。

说明：只移除了顶部展示，底层产品、来源和 Issue 数据仍保留；Case 结案前 Issue 校验逻辑仍存在。

思维导图 Case 根节点已移除旧 PAL 产品标签，避免与新产品 Pill 重复。

产品中心已移除：

- 产品列表中的 ObjectType Pill。
- 产品详情头部的 ObjectType Pill。
- 产品详情头部的“当前阶段”Pill。

产品中心仍保留：

- ObjectType 筛选器。
- 产品生命周期阶段条。
- 项目阶段范围。
- Checklist 所属阶段。

---

## 8. 主要文件清单

### 核心修改

- `app/src/app/components/CreateCaseMindMapFlow.tsx`
- `app/src/app/components/MindMapFlow.tsx`
- `app/src/app/components/CreateCase.tsx`
- `app/src/app/components/data.ts`
- `app/src/app/components/TaskDetail.tsx`
- `app/src/app/components/CaseDetail.tsx`
- `app/src/app/components/AddTaskModal.tsx`
- `app/src/app/components/MindMap.tsx`
- `app/src/app/components/VoiceNote.tsx`
- `app/src/app/components/AttachmentInput.tsx`

### 产品 Pill

- `app/src/app/components/CaseProductPill.tsx`（新增）
- `app/src/app/components/CaseList.tsx`
- `app/src/app/components/MyTodo.tsx`
- `app/src/app/components/Dashboard.tsx`
- `app/src/app/components/CategoryTimeline.tsx`
- `app/src/app/components/ProfilePage.tsx`
- `app/src/app/components/MyApprovals.tsx`
- `app/src/app/components/DemoMode.tsx`
- `app/src/app/components/ProductCenter.tsx`

### 生成产物

- `app/dist/`
- `app/EA系统-离线版.html`
- `web/`
- `V1.0.3.html`

---

## 9. 构建与发布

本项目没有测试框架、Lint 或 Formatter。当前验证方式是 TypeScript 诊断、Vite 构建和浏览器交互验证。

发布命令：

```bash
cd app
npx vite build --base=./
rm -rf ../web && cp -R dist ../web
node inline-build.mjs
cp EA系统-离线版.html ../V1.0.3.html
```

最新构建已成功：

- Vite 6.3.5。
- 1800 modules transformed。
- 无 TypeScript 错误。
- 仅存在既有的大 chunk 警告，不影响运行。

打开方式：

- 离线演示：双击根目录 `V1.0.3.html`。
- Web 工程产物：使用静态服务器打开 `web/`，不要用 `file://` 直接打开模块版 `web/index.html`。

---

## 10. 建议测试清单

### 新手引导

- [ ] 清除 `ea_mm_create_spotlight_v8` 后，进入新建 Case 立即出现引导。
- [ ] 14 个步骤顺序正确。
- [ ] 全屏、缩略图、左下角三个控制按钮均能正确高亮。
- [ ] “因为”和“所以”必填控制正确。
- [ ] 保存思路后不会自动创建任务。
- [ ] 点击真实蓝色加号后只创建一个任务。
- [ ] 保存任务后先收卡，再平滑移动至图例。
- [ ] 图例、连接线、任务卡色条同时高亮。
- [ ] 图例到色条的连线在横向和纵向布局下均正确。
- [ ] 添加更多并列思路和子思路步骤不会遮挡目标卡片。
- [ ] 勾选“下次不再显示”后关闭，第二次进入不再出现。
- [ ] 取消勾选后关闭，第二次进入仍出现。

### 思维导图

- [ ] 新建 Case 与我的 Case 工具栏一致。
- [ ] 两边图例均始终显示，且没有图例隐藏按钮。
- [ ] 缩略图颜色一致。
- [ ] 任务卡显示态和编辑态一致。
- [ ] 编辑节点不会覆盖其他节点。
- [ ] 横向/纵向布局、筛选、折叠和重置正常。
- [ ] 圆角连线无明显重叠或扭曲。
- [ ] 附件和语音备注可正常保存与播放。

### 产品 Pill

- [ ] 所有 Case L 标签旁都有产品 Pill。
- [ ] 已登记产品显示正式产品 code。
- [ ] 未登记产品回退显示 Case 的原始 product。
- [ ] 无产品时显示“未关联产品”。
- [ ] Case 详情顶部不再出现 PAL/FSO/Die/来源/Issue 标签组。
- [ ] 产品中心不再出现头部 ObjectType/当前阶段 Pill，但生命周期阶段条仍正常。

---

## 11. 已知风险与后续建议

1. **引导版本持久化**  
   每次变更引导流程都需要升级 `MM_CREATE_SPOTLIGHT_KEY`。否则测试人员已勾选“不再显示”后，会误以为功能消失。

2. **图例连线定位**  
   当前通过 DOM `getBoundingClientRect()` 和 `data-create-guide` 锚点计算。需要重点测试全屏、窗口缩放、横向/纵向布局和浏览器缩放比例。

3. **产品 Pill 覆盖面**  
   已覆盖主要 Case UI。未来新增任何 L 标签时应直接复用 `CaseProductPill`，避免再次出现 L 标签与产品信息分离。

4. **无 Git 元数据**  
   当前目录不是 Git 仓库，无法提供 commit 或精确 diff。建议开发同事接手后立即纳入 Git，并将本文件作为首个交接基线。

5. **大包警告**  
   当前单 JS bundle 约 1.2 MB，Vite 有 chunk-size 警告。离线单文件交付模式下暂不影响使用，但 Web 版本后续可考虑按页面动态加载。

---

## 12. 给开发同事的最短阅读路径

建议按以下顺序阅读：

1. `app/CLAUDE.md`：工程约束和构建方式。
2. `app/src/app/components/CreateCaseMindMapFlow.tsx`：新建 Case 导图和全部新手引导。
3. `app/src/app/components/MindMapFlow.tsx`：我的 Case 导图基准实现。
4. `app/src/app/components/CreateCase.tsx`：新建 Case 主流程和 ThoughtGroup/ActionCard 数据结构。
5. `app/src/app/components/data.ts`：Case、Task、Product、Issue mock 数据模型。
6. `app/src/app/components/CaseProductPill.tsx`：新的产品 Pill 统一组件。
7. `app/inline-build.mjs`：离线单文件生成逻辑，禁止把 `String.replace` 的函数替换改成字符串替换。

---

## 13. 【前端开发必看】可复用代码包

为方便前端同事直接阅读和迁移，已将本次思维导图及新手引导相关源码复制到根目录：

`mindmap/`

> 注意：这是 2026-07-14 的源码快照和分享副本，不是 EA 工程的运行源目录。继续修改当前工程时，仍应编辑 `app/src/app/components/` 下的正式源码。

### 13.1 目录内容

| 文件 | 前端可复用内容 |
|---|---|
| `mindmap/CreateCaseMindMapFlow.tsx` | 新建 Case 思维导图、14 步 Spotlight、新手引导状态、目标定位、过渡动画、图例与任务卡紧急度色条联动 |
| `mindmap/MindMapFlow.tsx` | 我的 Case 正式导图、节点卡片基准、圆角连线、共享主干、编辑态避让、工具栏和缩略图 |
| `mindmap/CreateCase.tsx` | `ThoughtGroup`、`ActionCard`、新建 Case 与导图的数据传递 |
| `mindmap/data.ts` | `CaseItem`、`TaskNode`、`Urgency`、`urgencyBar`、`levelColors` 等数据模型和颜色映射 |
| `mindmap/AttachmentInput.tsx` | 任务附件输入和紧凑模式 |
| `mindmap/VoiceNote.tsx` | `VoiceRecorder`、`VoicePlayer` |
| `mindmap/StatusBadge.tsx` | 任务状态图标和统一状态样式 |
| `mindmap/CaseProductPill.tsx` | 始终伴随 L 层级标签的产品 Pill |
| `mindmap/README-前端复用说明.md` | 迁移说明、依赖、阅读顺序和验证重点 |

### 13.2 最值得直接抽取的代码

前端实现思维导图新手引导时，优先从 `CreateCaseMindMapFlow.tsx` 抽取：

- `CoachStep`：引导步骤类型。
- `COACH_STEPS`：步骤顺序。
- `COACH_CONTENT`：标题、文案和目标选择器。
- `CreateCaseSpotlight`：遮罩、高亮、浮窗、进度、定位和动画。
- `data-create-guide`：DOM 目标锚点约定。
- `MM_CREATE_SPOTLIGHT_KEY`：首次展示和“不再显示”版本控制。
- 图例与任务卡紧急度色条之间的 SVG 高亮连线。
- 思路保存、手动添加任务、任务保存等真实操作驱动的步骤推进逻辑。

### 13.3 推荐工程化拆分

如果前端同事要把该方案落入正式工程，建议拆成：

- `MindMapSpotlight.tsx`：Spotlight 视觉和定位。
- `mindMapGuideSteps.ts`：步骤配置。
- `useMindMapGuide.ts`：步骤状态、持久化、真实操作推进。
- `UrgencyLegend.tsx`：图例和任务卡紧急度联动。
- `mindMapLayout.ts`：节点布局与边路由。

### 13.4 依赖说明

这批文件不是独立 npm 包，主要依赖：

- React 18。
- TypeScript。
- `@xyflow/react`。
- `lucide-react`。
- Tailwind CSS v4。
- EA 工程中的部分业务组件和 Store。

迁移时建议先抽取 `CreateCaseSpotlight`、引导步骤配置和状态 Hook，不建议直接把整个 `CreateCase.tsx` 搬入其他工程。
