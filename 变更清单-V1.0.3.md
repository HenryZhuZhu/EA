# V1.0.3 变更清单（供检查）

> 本轮围绕「产品 → 项目 → Checklist(WBS) → 检查项 → Case → Issue」这条新领域模型，对 EA 平台做了一次大改。
> 打开 `V1.0.3.html`（首次进入若弹新手引导，先关掉）逐条核对。每项前面的 `[ ]` 可当勾选框。

---

## 0. 新增 / 修改的文件一览

**新增文件**
- `app/src/app/components/domainStore.ts` — 领域可变状态（Issue / 项目·检查项 / Case↔Case 弱链接 / Fail 派生 Case / 人工立案 Case），localStorage 持久化。
- `app/src/app/components/caseInsights.ts` — 本地启发式「AI」：Case 相似度、建议关联、建议归纳成簇。
- `app/src/app/components/ProductCenter.tsx` — Checklist 管理（产品 → 项目 → Checklist(WBS) → 检查项）。
- `app/src/app/components/IssueCenter.tsx` — Issue 详情（现仅剩详情）。
- `app/src/app/components/IssueGroupView.tsx` — Issue 中心「按 Case 分组」泳道视角。
- `app/src/app/components/IssueHub.tsx` — Issue 中心（双视角容器）。
- `app/src/app/components/CaseRelations.tsx` — Case 详情的「归属 Issue + 关联 Case + AI」关系区。

**主要修改文件**
- `app/src/app/components/data.ts` — 领域模型类型 + Mock（阶段/产品/项目/Checklist/检查项/Issue/弱链接）。
- `app/src/app/App.tsx` — 路由 / 面包屑 / 视图。
- `app/src/app/components/Sidebar.tsx` — 导航项与命名。
- `app/src/app/components/CaseList.tsx` — 「分析中心」枢纽 + 子视图。
- `app/src/app/components/CaseDetail.tsx` — 产品/来源/Issue 徽章、关系区、结案闸门、解题路径。
- `app/src/app/components/CreateCase.tsx` — 来源选择 + 归属 Issue + 真实持久化。
- `app/src/app/components/CloseCaseModal.tsx` — 结案勾选解题路径。
- `app/src/app/components/CategoryTimeline.tsx` — 产品阶段替换为真实 8 段。
- `app/src/app/components/ContestView.tsx` — 大赛改名。

---

## 1. 导航与命名（Sidebar / App）
- [ ] 左侧栏原「Case 中心」→「**分析中心**」。
- [ ] 左侧栏**已移除**「产品监控」「Issue 库」两个顶层入口（并入分析中心）。
- [ ] 左侧栏原「PTE AI 大赛」→「**AI 创新大赛**」，右侧带**红色实底「Hot」王冠 Tag**（原渐变太浅已改）。
- [ ] 面包屑随页面正确变化，无死链。

## 2. 分析中心（枢纽 · CaseList）
- [ ] 聚焦页标题「分析中心」，入口卡：全部 Case 列表 / 高优 / 今日 / 产品分类 / 平台分类 / **Issue 中心** / **Checklist 管理**。
- [ ] 点各入口后**页面标题各不相同**（全部 Case 列表 / Issue 中心 / Checklist 管理 / 产品分类 / 平台分类），不再都叫"分析中心"。
- [ ] 每个子视图左上角只有**一个「返回」**。

## 3. Checklist 管理（WBS × Checklist 融合）· ProductCenter
- [ ] 进入不出现重复的「产品·项目监控中心」标题（内嵌时隐藏）。
- [ ] 选产品 → 项目 → Checklist **可任意层级递归展开**（示例最多 3 层）。
- [ ] **只有叶子**检查项有状态下拉（未开始/进行中/Pass/Fail/N/A）；**父节点显示「汇总·X」**、无下拉、按后代叶子自动汇总。
- [ ] 把某叶子改 **Fail** → 出现「**查看 Case**」（不是 Issue）。
- [ ] 点「查看 Case」→ 打开**自动生成的 Case**（名称含"检查项失败"、来源·检查项Fail、未关联 Issue）。

## 4. Case 详情（CaseDetail + CaseRelations）
- [ ] 头部下方有 **产品 / objectType / 来源 / 归属 Issue** 徽章；未关联 Issue 时显示琥珀色「未关联 Issue」。
- [ ] **归属 Issue（多对多）**：可「关联到已有 Issue」「新建 Issue 并关联」；可关联**多个**；每个可单独「取消该归属」；显示**兄弟 Case**。
- [ ] **关联 Case（弱链接）**：可加「重复/关联/起因/阻塞」；「**AI 建议**」列出相似 Case（相似度%+理由+建议关系），可一键「挂链」。
- [ ] **结案闸门**：未关联 Issue 时点「结案」→ 拦截提示「结案前需先关联 Issue」，不进入结案。
- [ ] 关联后可结案；结案弹窗有「**解题路径**」勾选（勾有效任务）。

## 5. 新建 Case（CreateCase）
- [ ] 顶部有**说明**：检查项失败自动建 Case；此处人工立案；结案前必须关联 Issue。
- [ ] 底部「**Case 来源与归属 Issue**」区块：
  - [ ] **来源**：手动/临时排查、Q·FAQA 客诉、MC·WBS、SC·FAE（选客诉时提示填客户）。
  - [ ] **归属 Issue** 上方有**查找/过滤**（搜索编号/名称 + objectType + 状态）；可多选，或填「新建 Issue 名称」。
- [ ] 产品下拉为真实产品清单。
- [ ] 填必填项后点「确定」→ **真的生成 Case（编号 EA-M-\*）并跳进该 Case**，来源与所选 Issue 都带上。
  - 附：修复了一个潜在 Bug——旧「新建 Case」因校验一个不存在的字段而**永远无法提交**、且**只导航不创建**。

## 6. Issue 中心（IssueHub）· 双视角合并
- [ ] 一个「**Issue 中心**」，顶部切换：**按 Case 分组** / **Issue 列表**。
- [ ] **按 Case 分组**：过滤（搜索 + 层级 + 紧急度 + 产品）→ Issue 泳道 + 未归纳 Case；**AI 建议归纳** banner（一键归纳）；未归纳 Case **多选 → 归纳为一个 Issue**。
- [ ] **Issue 列表**：概览统计（全部/调查中/已定位/已解决/已同步 RETs）+ 筛选（状态/来源/objectType/搜索）+ Issue 列表。
- [ ] 两个视角点任一 Issue → **同一个 Issue 详情**；详情返回按钮「返回 Issue 中心」。
- [ ] 旧「共性问题 · Issue 库」列表已**删除**（无幽灵页）。

## 7. Issue 详情（IssueCenter → IssueDetail）
- [ ] 展示：共性描述 / 根因 / 解决方案 / 来源链 / 归集 Case（含解题路径）/ 受影响产品 / **fanout 扩散** / **RETs 同步**。
- [ ] **提交解决方案** → 系统**强制 fanout**：为所有其它产品（含 MP、排除源产品）各建 1 个验证 Case，并把方案**反写为新检查项**。
- [ ] **标记已解决**（需全部成员 Case 已给解题路径）→ 才能「**同步 RETs**」（仅已解决可用）。

## 8. AI 能力（caseInsights，本地启发式 Mock）
- [ ] Case 相似度 = 0.35 同 objectType + 0.15 同产品 + 0.3 失效模式文本 + 0.1 阶段 + 0.1 平台。
- [ ] **建议关联**：≥70% 建议「重复」，否则「关联」（DIMM 子案上可验证）。
- [ ] **建议归纳**：把互相高相似的 Case 成簇建议归为一个 Issue，并自动起名。

## 9. 领域模型 / 新增逻辑（贯穿全局，供理解）
- [ ] **层级**：产品 → 项目（挂阶段，可跨阶段）→ Checklist(WBS 递归树) → 具体检查项。
- [ ] **8 阶段主干**：DKO → AR → FVR → DBR → FSO → ES → CS → MP。
- [ ] **Case = 唯一原子创建单元**（5 入口：人工新建 / 检查项 Fail / DIMM 拆分 / fanout 验证 / 外部 MC·WBS·SC·FAE）。
- [ ] **Issue 只从 Case 归纳而来**（Case 详情新建 / 归纳视图多选 / AI 建议），不凭空建空 Issue。
- [ ] **Case ↔ Issue 多对多**；**结案前必须 ≥1 Issue**。
- [ ] **Case ↔ Case 弱链接**（重复/关联/起因/阻塞，多对多，仅线索）。

---

## 10. 占位 / 待补（非 Bug，等你给真实内容再替换）
- [ ] 固定**项目模板 / Checklist / 检查项**清单为代表性示例（`data.ts` 中 `PROJECT_TEMPLATES`，标注 ⚠️占位）。
- [ ] **RETs 接口规格**未定（现只做"已解决→单向推送"的状态与按钮）。
- [ ] **SC·FAE 现场系统名**未定（现显示「（待命名）现场系统」）。
- [ ] Issue 中心 / 新建 Case 的**过滤条件**为初版（可增删）。

---

## 11. 构建 / 交付
- [ ] 源码在 `app/`；交付物为 `web/`（分离式）与 `V1.0.3.html`（单文件离线版），二者均由 `app/` 重新生成。
- 构建命令：`cd app && npx vite build --base=./ && rm -rf ../web && cp -R dist ../web && node inline-build.mjs && cp EA系统-离线版.html ../V1.0.3.html`
