# EA Platform · V1.0.3 设计文档

> 主题：**管理者看板升级 + 返工/复发模型 + 状态时间戳地基 + 组织架构**。
> 本版本变动较大，独立于 V1.0.2（V1.0.2 冻结，相关产物保留在 `EA V0.2/V1.0.2/`）。
> 本目录存放 V1.0.3 的设计文档、离线 HTML 与变更源码快照。

---

## 0. 已锁定决策（D 记录）

| 编号 | 决策 |
|---|---|
| D-1 | 看板**一套数据三种形态**：运营台（主管）/ 决策台（高层自助实时）/ 决策简报（高层定期推送）。不做割裂的两套产品。 |
| D-2 | 高层不止一层：**总监=周报，在「我的」内查看；VP=月报，邮件推送**。决策台/简报按管理者**分管范围 scope**。 |
| D-3 | 组织架构无真实数据，**采用占位 Mock**（工程师→主管→总监→VP），见 §2。 |
| D-4 | 返工/复发：保留"已完成不可复开"，改以**「退回重做」动作**新建任务并溯源（`reworkOf`）；返工链判定 **`reworkOf` 显式链为主、同思路节点序列为辅校验**。 |
| D-5 | 「退回重做」发起权限：**上游 / 下游 / Case Owner**（均需填理由）。 |
| D-6 | 给任务加 **`statusHistory`（状态时间戳）** 作为响应时长 / 阶段停留 / SLA / Aging 的地基。 |

---

## 1. 需要补的管理者关注面（R 需求）

在 V1.0.2 的"效率（Cycle Time）+ 激励（积分）"之外，补：

| R | 关注面 | 关键指标 |
|---|---|---|
| R-A | **健康度·现状** | 在办量、L1/L2 关键、逾期/临期、积压、SLA 按时率 |
| R-B | **质量** | 闭环率、**返工率/复发率**、无效结论率、一次通过率 |
| R-C | **风险** | 紧急度×等级风险矩阵、客户升级、停滞>N 天卡死案 |
| R-D | **人·负载** | 负载均衡、谁超载、**派单响应时长** |
| R-E | **系统性失效洞察** | 失效模式/产品/工序/客户分布 + Pareto + 反复试错方向 |
| R-F | **可下钻 + 可行动** | 指标→具体 case/人；一键催办/升级/改派 |

---

## 2. 组织架构（占位 Mock，已落 `data.ts`）

四层：**工程师 → 主管 → 总监 → VP**。

- **VP**：林副总（分管全公司）。
- **总监**（3 条线）：
  - 高总监 · **测试总监** → 分管 PTE / CP / FT / DA
  - 马总监 · **工艺总监** → 分管 PE / QA / EFA
  - 赵总监 · **设计总监** → 分管 RD / PM
- **主管**：沿用 `departmentSupervisors`（9 部门各 1）。
- **工程师**：沿用 `ownerToDepartment` 中人员。

已提供的派生能力（`data.ts`）：
- `orgRole(name)` → VP / 总监 / 主管 / 工程师
- `reportsTo(name)` → 直接上级
- `managerDeptScope(name)` → 管理者分管的部门集合（看板/简报 scope）
- `deptToDirector` / `directors` / `vp`
- `briefingPolicy(name)` → 总监{周报,平台} / VP{月报,邮件} / 其余 null

---

## 3. 返工 / 复发模型

### 3.1 动作：退回重做
- 触发者：上游 / 下游执行者 / Case Owner（D-5），**必须填退回理由**。
- 行为：**新建一个执行任务**（不复开旧任务，守住防刷），自动：
  - `reworkOf = 原任务id`，`reworkReason = 理由`；
  - 预填上下文（目标/预期），名称预填"（重做）原任务名"（可改名，不影响判同）；
  - 退回理由写入 Case 流转日志；
  - 新任务挂在**与源任务同一"思路/怀疑方向"节点**下。
- 触发信号补充：源任务"结论无效/部分有效"是返工的强信号。

### 3.2 返工链判定（D-4）
- **主**：沿 `reworkOf` 回溯形成显式链；链长 ≥2 即存在返工。
- **辅**：同一思路节点下"已完成（尤其结论无效）后又新增执行任务"作为校验/补充识别（应对历史未走退回动作的情况）。

### 3.3 指标定义
- **返工率/复发率** = 含返工链的目标数 ÷ 总目标数。
- **平均试错深度** = 各思路方向下执行任务数均值（越深=越难，候选专项）。
- **无效结论率** = 结论无效/部分有效的完成任务占比。

### 3.4 可视化
- 思维导图：同思路下重试链以特殊连线/角标标"第 N 次"。
- 任务详情：显示"返工来源 / 后续重做"溯源。
- 反哺 R-E：被反复试错的怀疑方向高亮为"系统性难题"。

---

## 4. 状态时间戳 statusHistory（地基，已落类型）

- 模型：`TaskNode.statusHistory: { status; at; by? }[]`，每次状态变更追加。
- 解锁：
  - **派单响应时长** = "待接受" → "进行中/接受" 的时长。
  - **各阶段停留** = 受理 / 分析 / 验证 / 结案 各段时长。
  - **SLA 达成 / Aging 分桶** 的时间基准。
  - Cycle Time **拆段**（看卡在哪一段，而非只有总时长）。

---

## 5. 看板形态（D-1 / D-2）

| 形态 | 受众 | scope | 呈现 | 可行动 |
|---|---|---|---|---|
| 运营台 | 主管 | 本部门 | 实时、个人/单 case 粒度 | 下钻 + 催办/升级/改派 |
| 决策台 | 总监/VP（自助） | `managerDeptScope` | 实时、部门/全局、结论先行 | 只读洞察 + 下钻 + 导出 |
| 决策简报 | 总监(周)/VP(月) | `managerDeptScope` | 叙述化快照（同指标引擎） | 平台查看 / 邮件推送 |

- 简报 = 决策台快照 + 自动叙述，**复用同一指标口径**，不另算。
- 默认首屏按 `orgRole` 自适应。

---

## 6. 落地顺序（建议）

1. ✅ **数据地基**：`TaskNode.statusHistory` + `reworkOf/reworkReason` 类型；组织架构 Mock 与 scope/简报 helper（均已落 `data.ts`）。
2. ✅ **样例数据**：c001 `thought1` 下补返工链样例（`t1c` 结论无效 → `t1c_r2` 第 2 次重做）+ 多个任务 `statusHistory`。
3. ✅ **交互**：任务详情「退回重做」动作 + 思维导图返工链可视化 + 任务详情返工链溯源条（详见 6.1）。
4. ⏳ **指标层**：返工率 / 响应时长 / 阶段停留 / SLA 计算（基于地基）。
5. ⏳ **看板**：运营台 / 决策台 / VP 简报样式已各出 10 版（本目录 `样式探索-*-10版.html`），待选定后接进 React，按 `orgRole` 自适应首屏。

### 6.1 退回重做 · 实现要点（已落地）

- **入口**：任务详情页，执行任务 `已完成` 时底部出现「退回重做」；结论 `无效/部分有效` 时高亮强信号提示。
- **动作**：弹窗必填退回理由（含 4 个预设）→ **另建一个执行任务**（不复开旧任务，守防刷），`reworkOf=源id`、`reworkReason=理由`、命名「（重做）原名」、默认负责人=源负责人、状态 `待接受`、写入 `statusHistory`；自动挂在**与源任务同一思路节点**下；理由写入 Case 流转日志（`timeline`）。
- **判定/计数**：`data.ts` 新增 `indexTasks / reworkSeqOf / reworkChildrenOf / inReworkChain`，沿 `reworkOf` 显式链回溯「第 N 次」。
- **可视化**：思维导图同思路下源→重做加**橙色虚线**「退回重做·第 N 次」，重做节点角标「返工·第 N 次」、源节点角标「已被重做」；任务详情**返工链溯源条**（本任务重做自 … / 后续重做 …，可点击跳转）。
- **权限（D-5）**：上下游 / Case Owner；演示版对 `已完成` 执行任务开放入口。

---

## 7. 本版本变更摘要（V1.0.3 起步）

- `data.ts`：`TaskNode` 新增 `reworkOf` / `reworkReason` / `statusHistory`；新增组织架构 Mock（`vp` / `directors` / `deptToDirector` / `orgRole` / `reportsTo` / `managerDeptScope` / `briefingPolicy`）。
- 版本号：侧栏 → V1.0.3。
- 后续按 §6 推进；每步发布离线 HTML 至本目录 `V1.0.3.html` 并同步源码快照。
