## 什么是领域模型

领域模型可以简单理解为：

> **产品对现实业务世界的结构化表达。**

它回答的不是“页面怎么排”，而是：

- 业务中有哪些核心事物？
- 每个事物代表什么？
- 它们有哪些属性和状态？
- 它们之间是什么关系？
- 哪些行为是允许的？
- 哪些业务规则必须始终成立？

在 EA 中，Case、Issue、产品、项目、Checklist、检查项、思路、任务、实验、证据和解决方案，都属于领域对象。

---

## 用 EA 举一个例子

现实业务是：

> 某产品的检查项 Fail，工程师展开分析，执行多个实验，找到根因和解决方案；之后把同类 Case 归纳为 Issue，并在其他产品上验证。

领域模型需要把这句话拆成明确对象：

```mermaid
flowchart LR
    Product[产品] --> Project[项目]
    Project --> Checklist
    Checklist --> CheckItem[检查项]
    CheckItem -->|Fail 自动创建| Case
    Case --> Analysis[分析思路]
    Analysis --> Experiment[实验/执行任务]
    Experiment --> Evidence[证据]
    Evidence --> Conclusion[结论]
    Case -->|归纳| Issue
    Issue --> RootCause[根因]
    Issue --> Solution[解决方案]
    Solution --> Fanout[跨产品验证]
    Solution -->|沉淀| Knowledge[工程知识]
    Solution -->|回写| CheckItem
```

这张图表达的是业务结构，而不是页面导航结构。

---

## 领域模型不是数据库表

领域模型和数据库结构有关，但两者不完全相同。

### 领域模型关心

- 一个 Case 到底代表什么？
- Case 与 Issue 是一对一，还是多对多？
- Case 什么时候可以结案？
- 根因属于 Case 还是 Issue？
- 实验结论与证据是什么关系？
- Checklist Fail 后应该创建 Case 还是 Issue？
- fanout 是解决方案的验证，还是独立分析？
- AI 建议能否直接改变业务状态？

### 数据库关心

- 数据存在哪张表
- 主键是什么
- 如何建立索引
- 如何提高查询性能
- 如何处理事务

应该先确定业务含义，再设计数据库。否则很容易让页面和数据库反过来定义业务。

---

## 领域模型也不是页面结构

例如页面上可能有一个“新建 Case”表单，里面同时填写：

- 产品
- 失效信息
- 来源
- 负责人
- Issue
- 分析思路

但这不代表这些内容都属于 Case。

可能的真实关系是：

- 产品是独立实体
- 失效现象属于 Failure Observation
- 来源属于 Source Reference
- 负责人属于 Assignment
- Issue 是独立的共性问题
- 分析思路属于 Analysis Node

页面为了操作效率可以把它们放在一起，但领域模型必须保持边界清晰。

---

# 一个领域模型包含什么

## 1. 实体 Entity

实体是有唯一身份、会持续变化的对象。

例如：

- Product
- Project
- Case
- Issue
- Task
- Experiment
- User

即使名称、负责人或状态变化，同一个 Case 仍然是同一个 Case。

---

## 2. 值对象 Value Object

值对象没有独立身份，主要表达一个业务值。

例如：

- 紧急度
- 失效位置
- 温度范围
- 电压条件
- 时间区间
- 测试条件
- 置信度
- 产品研发阶段

两个测试条件的字段完全相同时，可以认为它们表达同一个值；不需要给每个测试条件分配独立业务身份。

---

## 3. 关系 Relationship

需要明确对象之间的数量和方向。

例如：

- 一个 Product 可以有多个 Project
- 一个 Project 可以有多个 Checklist
- 一个检查项 Fail 可以创建一个 Case
- 一个 Case 可以关联多个 Issue
- 一个 Issue 可以归纳多个 Case
- 一个 Case 可以包含多个分析分支
- 一个解决方案可以产生多个 fanout 验证 Case

关系不能只靠页面上的链接暗示，必须成为正式业务定义。

---

## 4. 状态 State

每个重要对象有哪些状态，以及如何变化。

例如 Case：

```mermaid
stateDiagram-v2
    [*] --> 草稿
    草稿 --> 待处理: 提交
    待处理 --> 分析中: 接受
    分析中 --> 待结案: 提交结论
    待结案 --> 已结案: 审批通过
    待结案 --> 分析中: 退回
    分析中 --> 已中止: 中止
```

状态模型需要回答：

- 谁可以执行状态转换？
- 转换前需要满足什么条件？
- 转换后要触发什么行为？
- 是否允许撤销？
- 是否需要审批？

---

## 5. 业务规则 Invariant

Invariant 是系统无论如何都必须保持成立的规则。

EA 当前可以定义的规则包括：

- Case 结案前必须至少关联一个 Issue
- Case 结案时必须指定有效解题路径
- 只有 Checklist 叶子检查项才能填写 Pass、Fail 或 N/A
- 检查项 Fail 创建 Case，不直接创建 Issue
- Issue 必须从一个或多个 Case 中归纳形成
- Issue 提交解决方案时必须启动 fanout
- fanout 验证 Case 必须保留来源 Issue
- 已解决 Issue 才能同步到 RETs
- AI 不得在未经确认的情况下执行高风险状态流转

规则如果只散落在按钮事件和文案里，系统后续就会产生不一致。

---

## 6. 领域行为 Behavior

对象不只有数据，也有行为。

例如 Case 可以：

- 接受
- 指派
- 添加分析思路
- 创建实验任务
- 提交结论
- 关联 Issue
- 发起结案
- 退回重做

Issue 可以：

- 归纳 Case
- 确认共性
- 登记根因
- 提交解决方案
- 发起 fanout
- 标记已解决
- 同步 RETs

好的领域模型会表达“对象可以做什么”，而不只是保存一组字段。

---

## 7. 领域事件 Domain Event

领域事件表示业务中已经发生的事实。

例如：

- `CheckItemFailed`
- `CaseCreated`
- `CaseAssigned`
- `ExperimentCompleted`
- `RootCauseConfirmed`
- `CaseAssociatedWithIssue`
- `SolutionSubmitted`
- `FanoutStarted`
- `IssueResolved`
- `IssueSyncedToRETs`

事件非常适合 AI Native 平台，因为 AI 可以监听这些事件并主动工作。

例如：

```mermaid
flowchart LR
    Event[ExperimentCompleted] --> AI[AI 分析证据]
    AI --> Suggestion[建议下一步实验]
    AI --> Similar[查找相似 Case]
    AI --> Risk[提示结论冲突]
    Suggestion --> Human[工程师确认]
    Human --> Task[创建下一步任务]
```

---

# 为什么 EA 特别需要统一领域模型

## 1. 防止同一个概念在不同页面含义不同

例如“Issue”如果在一个页面代表客户问题，在另一个页面代表多个 Case 的共性问题，AI、搜索和统计都会混乱。

统一领域模型必须给出唯一解释：

> Issue 是多个 Case 所揭示的共性工程问题。

---

## 2. 防止业务逻辑散落

目前一些规则可能分别存在于：

- 新建 Case
- Case 详情
- Issue 中心
- Checklist 管理
- 结案弹窗
- fanout 操作
- 模拟数据状态层

如果缺少统一模型，同一个动作可能在不同入口产生不同结果。

---

## 3. 让 AI 真正理解业务

大模型读懂一段文字不等于理解企业业务。

AI 需要知道：

- Product 与 Case 的关系
- Case 与 Issue 的区别
- 实验和证据的区别
- 哪条结论已经验证
- 哪条只是工程师假设
- 哪个解决方案适用于哪些产品
- 哪个状态允许执行什么动作

领域模型相当于 AI 的“业务世界地图”。

---

## 4. 支撑未来系统集成

外部系统中的 Bug、8D、异常单、测试 Fail、WBS 项目，未必与 EA 对象一一对应。

只有 EA 先定义清楚自己的模型，才能判断：

- 外部对象映射到 Case 还是 Issue
- 外部状态如何映射
- 谁是数据责任方
- 哪些字段由 EA 修改
- 哪些字段只能从外部同步

---

# EA 领域模型最关键的几个问题

在设计完整模型前，建议先回答这些问题。

### Case 是什么？

建议定义：

> Case 是围绕一个具体产品、具体异常或具体验证目标开展的一次可结案工程分析。

需要明确它是否必须对应一个产品，以及 fanout Case、手动 Case、检查项 Fail Case 是否都属于同一种 Case。

### Issue 是什么？

建议定义：

> Issue 是从一个或多个 Case 中归纳出的、具有跨场景复用价值的共性工程问题。

需要明确一个 Case 是否可以属于多个 Issue。目前 EA 采用多对多关系。

### Task、Experiment 和 Evidence 是否相同？

它们不完全相同：

- Task：要求某人完成的工作
- Experiment：具有条件、步骤和结果的工程验证
- Evidence：支持或反驳判断的材料
- Conclusion：基于证据形成的判断

当前 EA 把很多内容集中在任务节点中。原型阶段可以，但面向真实系统时建议逐步拆开。

### 根因和解决方案属于谁？

建议：

- Case 可以形成局部结论和候选根因
- Issue 保存经过多个 Case 收敛后的共性根因
- 解决方案属于 Issue
- fanout 用来验证解决方案的适用范围

### AI 生成的内容是什么身份？

AI 输出不能直接等同于业务事实，建议区分：

- AI 建议
- 人工采纳的建议
- 尚未验证的假设
- 已有证据支持的结论
- 已审批的正式结论

这是 AI Native 系统非常关键的一层模型。

---

# 可以如何开始设计

建议不要先画数据库，而是制作一份“EA 领域模型说明”，分为五部分：

1. **领域词汇表**  
   明确定义 Product、Case、Issue、Experiment、Evidence、Solution 等术语。

2. **对象关系图**  
   表明对象之间的一对一、一对多和多对多关系。

3. **生命周期图**  
   分别描述 Case、Issue、Task、Solution 的状态变化。

4. **业务规则清单**  
   记录哪些规则在任何入口都必须成立。

5. **领域事件清单**  
   定义哪些业务事实会触发通知、自动化或 AI Agent。

一句话总结：

> **领域模型就是把半导体研发人员脑中的业务世界，变成团队、软件和 AI 都能共同理解并严格执行的一套语言、对象、关系与规则。**
